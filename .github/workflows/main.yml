name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install
      run: yarn
    - name: Run build
      run: yarn build
    - name: Run lint
      run: yarn lint
    - name: Run test
      run: yarn test
